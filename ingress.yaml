apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: message-ingress
  namespace: ingress-rest
spec:
  rules:
  - http:
      paths:
      - path: /status
        pathType: Prefix
        backend:
          service:
            name: rest-service
            port: 
              number: 80